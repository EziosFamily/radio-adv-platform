# 网电空间对抗性攻防算法验证平台

本平台用无线电调制识别数据（RML2016.10a 及抽样子集）做对抗攻防演示。浏览器打开后，主要使用两个页面：

- **攻击样本成功率测试**：进攻侧，看所选算法能不能把识别模型打错。
- **恶意数据检测率测试**：防守评估，看系统能不能把对抗样本挑出来。

启动步骤见同目录的 `启动方法.md`。先开后端 `run_flask.bat`，再开前端 `run_frontend.bat`，浏览器访问 http://127.0.0.1:5173 。

---

## 一、系统怎么用

### 1. 进入页面

打开前端后，顶部切换：

| 菜单 | 作用 |
|------|------|
| 攻击样本成功率测试 | 对信号加扰动，统计攻击成功率 |
| 恶意数据检测率测试 | 用同一批算法生成恶意样本，统计检测率与虚警率 |
| 网络拓扑中的关键节点筛选测试 | 演示拓扑关键节点，与信号攻防无关 |

### 2. 攻击页操作

1. **选择信号集**，点 **加载**。图上方的「信号数量」应等于该文件条数，例如 `radio3.pkl` 为 220。
2. **选择攻击方法**（FGSM、PGD、BIM 等）和 **模型**（CNN 或 RNN）。
3. 点图下方的 **攻击**，等步骤走完后看右侧结果。
4. 在信号图上 **点击某个点**，右侧出现该条信号的 I / Q / 幅度波形。攻击后会同时画出扰动前后的曲线。
5. 点 **重置** 回到刚加载的干净信号，可换算法或模型再打。

数据集越大（如 3000、5000 条），加载和攻击越慢，属正常现象。

### 3. 检测页操作

1. 同样先 **选择信号集** 并 **加载**，确认信号数量与数据集条数一致。
2. **选择检测方法**：这里列出的是具体攻击算法名（FGSM、PGD 等），表示「检测由该算法造出的恶意数据」，不是另一种独立检测器名称。
3. **选择模型**（CNN 或 RNN），用于生成对抗样本。
4. 点 **检测**，看右侧检测率、虚警率。
5. 点图上的信号点可看波形。点 **重置** 回到加载后的状态。

### 4. 信号集条数

| 文件 | 条数 |
|------|------|
| radio1.pkl | 1046 |
| radio2.pkl | 193 |
| radio3.pkl | 220 |
| random_rml2016_subset_300.pkl | 300 |
| random_rml2016_subset_500.pkl | 500 |
| random_rml2016_subset_1000.pkl | 1000 |
| random_rml2016_subset_2000.pkl | 2000 |
| random_rml2016_subset_3000.pkl | 3000 |
| random_rml2016_subset_5000.pkl | 5000 |

图上每个圆点对应一条 IQ 信号。点的大小固定；条数变多时绿框变高，可在图内滚动查看。

---

## 二、攻击和恶意检测分别指什么

两者可以用同一组算法（FGSM、PGD 等），但问的问题不同。

### 攻击

给干净信号加肉眼不易察觉的扰动，让调制识别模型把类别认错。

- **成功**：扰动后模型预测 ≠ 真实调制类别。
- **失败**：扰动后模型仍预测正确。
- **攻击成功率** = 成功条数 / 数据集条数。
- **输入特征的变化** = 扰动相对原信号的平均幅度比。

这是进攻评估：这套算法、这个模型，能不能被打穿。

### 恶意检测

先按所选算法，把数据集里每条信号都做成一条对抗样本（恶意数据），再判断系统能不能把它们标出来。

同一批信号会看两种状态：

- **未扰动**：干净样本，用来算虚警。
- **加扰动之后**：恶意样本，用来算检测率。

因此「对抗样本检出 x / 220」和「干净样本虚警 y / 220」的分母都是数据集条数，不是又多出一批数据。

| 指标 | 含义 |
|------|------|
| 对抗样本检出 | 扰动后的样本里，被标成恶意的条数 |
| 干净样本虚警 | 未扰动样本里，被误标成恶意的条数 |
| 恶意数据检测率 | 检出数 / 数据集条数 |
| 虚警率 | 虚警数 / 数据集条数 |

这是防守评估：对抗痕迹能不能被发现，会不会把正常信号误报成恶意。

---

## 三、信号图和结果怎么读

### 1. 加载之后（攻击页、检测页相同）

- 每个圆点是一条信号，颜色表示调制类别（QPSK、BPSK、QAM16 等）。
- 点下方的「信号 n」是编号，从 0 开始。
- 「信号数量: 已画点数 / 数据集条数」应对齐，例如 `220 / 220`。
- 点击圆点：右侧画出该条 I 路、Q 路或幅度波形。

### 2. 攻击之后

| 图上样子 | 含义 |
|----------|------|
| 左右两色各半个圆 | 攻击成功：左边是原来的类别色，右边是被认成的新类别色 |
| 整圆 + 红色方框 | 攻击失败：类别没被改掉 |
| 黄圈 | 当前点中的这条信号 |

右侧数字：

- **已处理样本**：本次攻击用到的条数，应等于数据集条数。
- **成功数量 / 总数量**：被打错的条数 / 总条数。
- **攻击成功率**：上一行的比例。
- 再点某个点时，会显示该条的编号和特征变化。

波形图中，蓝色多为扰动前，红色为扰动后，绿色为差值。

### 3. 检测之后

图例：

| 颜色 | 含义 |
|------|------|
| 绿色 | 对应的对抗样本未被标成恶意（漏检一侧偏绿） |
| 黑色 | 对应的对抗样本被标成恶意（检出） |
| 红框 | 该条判定有误（漏检或虚警） |

右侧数字按数据集条数统计，不再把干净样本和对抗样本加成两倍。

点击圆点后的波形标题会标明该条是检出、正常、漏检或虚警。

### 4. 重置

「重置」只恢复到本次 **加载** 后的干净图，不会自动换数据集。要换文件，重新选择信号集再点加载。
