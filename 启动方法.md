# 项目启动方法

本项目分前后端：后端是 Flask（`radioAdv`），前端是 Vue 3 + Vite（`173_vue/173_vue`）。请先启动后端，再启动前端。

| 服务 | 目录 | 默认地址 |
|------|------|----------|
| 后端 Flask | `radioAdv/` | http://127.0.0.1:5000 |
| 前端 Vue | `173_vue/173_vue/` | http://127.0.0.1:5173 |

---

## 一、环境准备（首次）

### 后端

- Python 3.11
- 项目根目录已有虚拟环境 `.venv` 时可直接使用
- 若虚拟环境不存在或依赖缺失：

```bat
cd /d d:\173
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements-backend.txt
.venv\Scripts\python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
```

### 前端

- 已安装 Node.js / npm
- 若 `173_vue\173_vue\node_modules` 不存在：

```bat
cd /d d:\173\173_vue\173_vue
npm install
```

---

## 二、启动后端

### 方式 1：双击脚本（推荐）

在项目根目录双击 `run_flask.bat`。

脚本会使用 `.venv\Scripts\python.exe` 运行 `radioAdv\app.py`，服务监听 `0.0.0.0:5000`。关闭该窗口即可停止后端。

### 方式 2：命令行

```bat
cd /d d:\173\radioAdv
d:\173\.venv\Scripts\python.exe app.py
```

启动成功后，浏览器访问 http://127.0.0.1:5000 应能连上 Flask。前端请求默认指向该地址（见 `173_vue/173_vue/src/api.js` 中的 `VITE_API_BASE`）。

---

## 三、启动前端

### 方式 1：双击脚本（推荐）

在项目根目录双击 `run_frontend.bat`。

脚本会进入 `173_vue\173_vue` 并执行 `npm run dev`，绑定 `127.0.0.1:5173`。关闭该窗口即可停止前端。

### 方式 2：命令行

```bat
cd /d d:\173\173_vue\173_vue
npm run dev
```

如需与脚本一致的本机绑定：

```bat
npm run dev -- --host 127.0.0.1 --port 5173
```

浏览器打开 http://127.0.0.1:5173 即可使用页面。

---

## 四、建议启动顺序

1. 先运行 `run_flask.bat`，确认后端已在 5000 端口监听。
2. 再运行 `run_frontend.bat`，打开 http://127.0.0.1:5173。
3. 两个窗口都不要关；关哪个窗口，对应服务就会停。

---

## 五、常见问题

- **提示 venv not found**：先按「环境准备」创建 `.venv` 并安装依赖。
- **提示 node_modules not found**：在 `173_vue\173_vue` 执行 `npm install`。
- **页面能开但接口失败**：确认 Flask 已启动，且前端请求的是 `http://127.0.0.1:5000`。
- **端口被占用**：后端改 `radioAdv/app.py` 末尾的 `port=5000`；前端用 `--port` 指定其他端口，并同步改前端 API 地址。
